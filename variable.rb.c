#include "port.h"
require 'port'

# IGNORE    rb_define_hooked_variable(name, var, 0, 0);
# IGNORE    rb_define_hooked_variable(name, var, 0, readonly_setter);

# IGNORE    rb_define_hooked_variable(name, 0, getter, setter);
# IGNORE    rb_define_const(rb_cObject, name, val);
